@page
@{
    ViewData["Title"] = L["portal.nav.privacy"].Value;
}
<section class="content-width text-page">
    <p class="eyebrow">@L["portal.nav.privacy"]</p>
    <h1>@L["portal.privacy.heading"]</h1>
    <p>@L["portal.privacy.body"]</p>
</section>